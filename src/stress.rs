use tokio;
use reqwest::{self, Client, Response, RequestBuilder, Method}; 

let ip [u8: 4] = [ 192, 18, 0, 0 ]

enum {
    Network,
    ExternalNetwork,
    Subnet,
    Port,
    Router,
    Sg,
    Fw,
    Tag,
    Lb,
}
pub fn run(worker: u16, number: u16, resource: )
